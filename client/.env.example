# Copy to .env to override the server endpoint.
# Local dev default is ws://localhost:2567 (no .env needed).
# In production use wss:// and your deployed server host:
# VITE_SERVER_URL=wss://your-server-host
